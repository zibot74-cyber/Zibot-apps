# ══════════════════════════════════════════
#  ZIbot — ملف الإعداد (.app_runtime)
#  انسخ هذا الملف إلى .app_runtime وعدّله
# ══════════════════════════════════════════

# ── Supabase (مطلوب) ─────────────────────
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── JWT Secret (مطلوب — 32+ حرف عشوائي) ─
JWT_SECRET=change-this-to-a-random-secret-at-least-32-chars

# ── AI Provider (groq أو claude) ─────────
AI_PROVIDER=groq
GROQ_API_KEY=gsk_...
# ANTHROPIC_API_KEY=sk-ant-...

# ── Facebook / Messenger (اختياري) ───────
# FB_VERIFY_TOKEN=my_verify_token
# FB_PAGE_TOKEN=EAAxxxxx
# FB_APP_SECRET=xxxxxxxx

# ── WhatsApp Cloud API الرسمية (مطلوب لتشغيل البوت على واتساب) ──
# من: developers.facebook.com → تطبيقك → WhatsApp → API Setup
WA_CLOUD_TOKEN=EAAxxxxx
WA_PHONE_NUMBER_ID=1234567890
WA_BUSINESS_ACCOUNT_ID=1234567890
# نص من اختيارك — يُستخدم لتأكيد الـ webhook في لوحة Meta
WA_VERIFY_TOKEN=choose-any-random-string
# WA_API_VERSION=v25.0

# ── البورت والبيئة ────────────────────────
PORT=3000
NODE_ENV=production
FRONTEND_URL=http://localhost:5173

# ── Stripe (الدفع ببطاقة) ─────────────────
# أنشئ حساباً على stripe.com/register
# sk_test_xxx = sandbox | sk_live_xxx = production
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── PayPal ────────────────────────────────
# أنشئ حساباً على developer.paypal.com
PAYPAL_CLIENT_ID=AXxxx...
PAYPAL_CLIENT_SECRET=EHxxx...
PAYPAL_MODE=sandbox

# ── البريد الإلكتروني (اختياري) ──────────
# Gmail:   smtp.gmail.com / 587 (استخدم App Password)
# Brevo:   smtp-relay.brevo.com / 587
# SendGrid: smtp.sendgrid.net / 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=ZIbot <your-email@gmail.com>

# ── وسائل الدفع اليدوي (للعرض في الداشبورد) ─
VODAFONE_CASH_NUMBER=01xxxxxxxxx
ORANGE_CASH_NUMBER=01xxxxxxxxx
ETISALAT_CASH_NUMBER=01xxxxxxxxx
INSTAPAY_NUMBER=01xxxxxxxxx
BANK_ACCOUNT_INFO=بنك مصر — 1234567890 — محمد أحمد

# ── Paymob (الدفع التلقائي بالمحفظة) ─────────
# أنشئ حساباً على: accept.paymob.com
# API Key:             Settings → Account Info
# Public Key:          Settings → Account Info
# Wallet Integration ID: Developers → Payment Integrations → (المحفظة الإلكترونية) → Integration ID
# HMAC Secret:         Developers → General Settings → HMAC Secret
# Webhook URL اضبطه على: https://yourdomain.com/api/payment/paymob/webhook
PAYMOB_API_KEY=ZXlKaGJHY2lPaUpJVXpVeE1pSXNJblI1Y0...
PAYMOB_PUBLIC_KEY=egy_pk_test_...
PAYMOB_WALLET_INTEG_ID=123456
PAYMOB_HMAC_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── CAPTCHA (Cloudflare Turnstile) — Fix #7 ───────────────
# للحصول على المفاتيح: https://dash.cloudflare.com/?to=/:account/turnstile
# Site Key يُضاف في الفرونت إند (متغير VITE_TURNSTILE_SITE_KEY)
# اتركه فاضياً أو NOT_USED لتعطيل التحقق (dev mode فقط)
TURNSTILE_SECRET=NOT_USED

