# ════════════════════════════════════════════════════════
#  ZIbot — متغيرات البيئة الكاملة
#  انسخ هذا الملف: cp env.text .app_runtime
#  ثم عبّئ الحقول المطلوبة
# ════════════════════════════════════════════════════════

# ── إلزامي ──────────────────────────────────────────────
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_KEY=eyJhbGci...service_role_key
JWT_SECRET=ضع_32_حرف_عشوائي_أو_أكثر_هنا

# ── الذكاء الاصطناعي ────────────────────────────────────
# اختر مزود واحد فقط
AI_PROVIDER=groq
GROQ_API_KEY=gsk_xxx...
# ANTHROPIC_API_KEY=sk-ant-xxx    ← فعّل هذا إذا AI_PROVIDER=claude

# ── واتساب Cloud API الرسمية ────────────────────────────
# من: https://developers.facebook.com → تطبيقك → WhatsApp → API Setup
# "Temporary access token" أو System User Token دائم
WA_CLOUD_TOKEN=EAAxxxxxxxx...
# Phone Number ID (رقم هوية رقم الهاتف) — من نفس الصفحة
WA_PHONE_NUMBER_ID=1234567890
# Business Account ID (WABA ID) — من WhatsApp Manager → Account tools
WA_BUSINESS_ACCOUNT_ID=9876543210
# نص عشوائي من اختيارك — ادخله أيضاً في خانة "Verify Token" عند ربط الـ webhook
WA_VERIFY_TOKEN=my_secret_verify_token_123
# App Secret — من Meta App Dashboard → App settings → Basic → App Secret
WA_APP_SECRET=abc123def456...
# إصدار Graph API (افتراضياً v25.0 — لا تغيّره إلا لو عندك سبب)
# WA_API_VERSION=v25.0

# ── ماسنجر (اختياري — اتركه إذا لم تستخدم ماسنجر) ──────
FB_VERIFY_TOKEN=NOT_USED
FB_PAGE_TOKEN=NOT_USED
FB_APP_SECRET=NOT_USED

# ── Paymob (الدفع التلقائي بالمحفظة المصرية) ───────────
# من: accept.paymob.com → Settings → Account Info & Developers
PAYMOB_API_KEY=ZXlKaGJHY2...
PAYMOB_PUBLIC_KEY=egy_pk_test_xxx...
# Integration ID خاص بالمحفظة — من Paymob → Developers → Payment Integrations
PAYMOB_WALLET_INTEG_ID=123456
# HMAC Secret — من Paymob → Developers → Webhooks
PAYMOB_HMAC_SECRET=abc123...

# ── وسائل الدفع اليدوي المحلي ───────────────────────────
VODAFONE_CASH_NUMBER=01XXXXXXXXX
ORANGE_CASH_NUMBER=01XXXXXXXXX
ETISALAT_CASH_NUMBER=01XXXXXXXXX
INSTAPAY_NUMBER=01XXXXXXXXX
BANK_ACCOUNT_INFO=بنك XXX - حساب XXXXXXXXX - اسم الحساب

# ── البريد الإلكتروني SMTP (اختياري لكن موصى به) ────────
# Gmail:  SMTP_HOST=smtp.gmail.com  PORT=587
# Brevo:  SMTP_HOST=smtp-relay.brevo.com  PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx
SMTP_FROM="ZIbot" <noreply@yourdomain.com>

# ── Stripe (اختياري — بوابة دفع دولية) ─────────────────
# من: https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_live_xxx...
# STRIPE_WEBHOOK_SECRET=whsec_xxx...

# ── PayPal (اختياري) ────────────────────────────────────
# PAYPAL_CLIENT_ID=AXxx...
# PAYPAL_CLIENT_SECRET=EXxx...
# PAYPAL_MODE=live

# ── URLs وإعدادات السيرفر ───────────────────────────────
PORT=3000
NODE_ENV=production
# عنوان داشبورد البوت (يُستخدم في CORS والإيميلات)
FRONTEND_URL=https://bot.yourdomain.com
